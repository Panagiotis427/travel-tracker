# Copy this file to .env.local and fill in from your Supabase project
# (Supabase dashboard -> Project Settings -> API).
# Leave BOTH unset to keep the app fully local (no accounts, no cloud).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
